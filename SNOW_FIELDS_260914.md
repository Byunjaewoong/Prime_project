# Snow-walker 필드 전환 — 2026-09-14

## 백업과 복원

현재 버전 `f19b283`을 `backup-260914-before-snow-fields` 브랜치로 보존하고 기존 GitHub 저장소에 push했다. 구현은 `snow-fields-260914` 브랜치에 보관한다.

작업 중 변경 사항이 없는 상태에서 기존 버전으로 전환한다.

```powershell
git switch backup-260914-before-snow-fields
npm run dev
```

구현본으로 돌아오려면 `git switch snow-fields-260914`를 실행한다.

## 동작과 표현

- 왼쪽 클릭/탭: 눈밭 → 초록 잔디 → 금빛 잔디 → 눈밭.
- 오른쪽 클릭: 클릭한 지면에 기존 랜덤 나무 모델을 추가한다. 캔버스의 우클릭 메뉴는 막는다.
- 바닥 색·풀결·요철·조명·안개·발자국 색이 약 0.6초에 걸쳐 전환된다. 빠르게 클릭하면 현재 표현에서 다음 목표로 이어진다.
- 인물 위치, 걷기 경로와 진행률, 애니메이션, 기존 발자국의 위치·수명, 추가한 나무는 전환 시 초기화하지 않는다. 기존 1,000개 발자국 제한과 페이드 정책은 유지한다.
- 세 필드의 카메라는 기존 먼 시점으로 고정한다. 잔디는 코드로 생성한 1,024px 텍스처의 짧은 풀결과 넓은 색 변화로 표현하며, 실제 풀 모델을 대량 추가하지 않는다.
- 눈밭과 잔디 텍스처를 셰이더에서 혼합해 같은 지면 메시와 재질을 재사용한다. 초록·금빛 필드는 같은 풀결을 공유한다.
- 참고 이미지는 구도와 색감 기준으로만 사용했다. 새 이미지 파일이나 패키지는 추가하지 않았다.

## 검증

프로덕션 빌드, 타입 검사, 린트(오류·경고 0)를 통과했다. Chrome 데스크톱 1280×800과 모바일 에뮬레이션 390×844에서 다음을 검사한다.

- 실제 코어를 실행하고 걷기만 잠시 멈춰 전환 전후 인물·경로·발자국의 객체와 위치, 진행률, 애니메이션 시간, 발자국 opacity, 카메라가 정확히 같은지 비교한다.
- 세 필드 순환과 모바일 탭 전환을 확인한다. 같은 위치·발자국으로 각 필드를 캡처한다.
- 60회 반복 전환 전후 지오메트리·텍스처 수와 scene 객체 수가 같음을 확인한다. 새 필드 전환에 따라 자원이 누적되지 않는다.
- 오른쪽 클릭으로 나무가 한 개 추가되고 필드는 바뀌지 않는지, 이후 필드 전환에서도 나무가 유지되는지 확인한다.
- 걷기 재개 후 진행률과 애니메이션 시간이 이어서 증가하는지 확인한다.
- 실제 Next.js 페이지의 필드 순환, 메뉴 안내, 홈 이동과 브라우저 오류를 검사한다.
- 종료 후 장면 지오메트리 수 0 및 WebGL 컨텍스트 종료를 확인한다. 스켈레톤의 bone texture와 셰이더의 잔디 텍스처도 해제한다. Three.js 모듈 내부의 공용 32×32 DFG LUT는 CPU 캐시에 남지만 전용 컨텍스트 종료로 해당 GPU 자원은 반환된다.

실제 휴대전화 성능 측정은 포함하지 않았다. 상세 결과와 화면은 Git에서 제외되는 `artifacts/snow-fields-260914/`에 보관한다. `comparison.html`에서 눈밭·초록·금빛 및 데스크톱·모바일을 선택해 비교할 수 있다.

## 재검증

Chrome과 별도의 Playwright 설치를 사용한다. 프로젝트 의존성은 추가하지 않는다. 로컬 프로덕션 서버를 실행한 뒤 다음 명령을 사용한다.

```powershell
node scripts/verify-snow-fields.mjs <Playwright 설치 경로>/package.json
```

다른 포트를 사용하면 먼저 `$env:VERIFY_BASE_URL="http://localhost:3001"`을 설정한다.
