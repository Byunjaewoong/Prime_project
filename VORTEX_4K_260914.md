# Vortex 형태 유지 및 4K 표현 개선

## 버전과 복원

구현 전 커밋 `58afedf`를 `backup-260914-before-vortex-4k` 브랜치로 보존했고 GitHub의 기존 저장소에도 업로드했다. 이전 유지보수 전 백업 `backup-260914`와 구분되는 새 백업이다.

구현 브랜치는 `vortex-4k-260914`다. 기존 버전을 확인하려면 작업 중 변경 사항을 커밋하거나 보관한 뒤 다음 명령을 사용한다.

```powershell
git switch backup-260914-before-vortex-4k
npm run dev
```

구현본으로 돌아오려면 `git switch vortex-4k-260914`를 실행한다. 패키지는 이번 변경에서 바꾸지 않았다.

## 구현

- CPU Solver의 배열 swap을 전체 복사 대신 필드 참조 교환으로 변경했다. 계산식, 순서, 반복 횟수 및 기존 물리 설정은 유지한다.
- 화면의 긴 변이 1920 CSS px를 넘으면 움직임 격자 증가를 제한하고 포인터 이동량을 기준 화면 크기로 보정한다. 1080p와 4K(16:9)에서 움직임 격자는 255×144다. 1920px 이하 화면은 기존 격자 설정을 유지한다.
- 기존 CPU RGB 색상장은 계속 계산한다. GPU 색상은 동일한 CPU 속도장을 따라 더 촘촘한 격자에서 이동시키고, 매 스텝 CPU 색상장으로 25% 보정한다. 기존 퍼짐과 잔상을 유지하면서 보간에 의한 뭉개짐을 줄이는 표현 방식이다.
- 기존 source, 시간 간격, 색상 감쇠 및 채도·밝기·0.75 지수의 tone curve를 사용한다. GPU 속도장 업로드는 CPU 색상 advection 당시의 drag 적용 전 값을 복원한다.
- 색상은 출력의 긴 변에 따라 최대 1024/2048, 면적은 2048×1152 픽셀로 제한한다. 4K(16:9)는 색상 2048×1152, 출력 3840×2160이다. 화면 출력도 4K 면적을 상한으로 관리한다.
- 입력 및 벡터 표시 Canvas와 GPU 색상 Canvas를 분리한다. 벡터가 꺼져 있을 때는 4K 투명 overlay를 매 프레임 지우지 않는다.
- WebGL2 또는 float framebuffer 미지원, 초기화 실패 및 context loss 시 기존 Canvas Renderer를 사용한다. CPU 색상장이 계속 유지되므로 fallback에서 새로 시뮬레이션을 시작할 필요가 없다.
- resize와 종료 시 texture/framebuffer/program을 해제한다. 기존 resize 시 시뮬레이션 초기화 동작은 유지한다.

## 검증

타입 검사, 린트(오류·경고 0), 프로덕션 빌드와 아래 검증을 실행했다.

### 계산 및 형태

백업 Solver와 최적화한 Solver를 31×23, 64×113, 255×144 격자에서 120스텝씩 비교했다. 입력, reset 및 설정 변경을 포함해 속도·RGB·source 배열의 모든 값이 정확히 일치했다.

기존 1080p 흐름을 기준으로 같은 정규화 입력을 100스텝 실행하고 원본 확대 출력과 고해상도 색상 출력을 비교했다. 4K 비교 원본은 **원본 1080p 흐름을 4K로 확대한 이미지**다. 기존 코드가 4K에서 별도로 생성하던 511×287 흐름과 픽셀 단위로 같다는 의미는 아니다.

| 화면 | 속도장 차이 | 색상 영역 겹침(IoU) | 표시 영역 RGB 평균 차이(0–255) |
| --- | ---: | ---: | ---: |
| 1920×1080 | 0 | 98.67% | 0.90 |
| 3840×2160 | 0 | 98.54% | 1.00 |
| 390×844 | 0 | 99.16% | 1.90 |

영역 겹침은 RGB 최댓값 8을 넘는 픽셀을 2px 간격으로 샘플링해 계산했다. 색상 중심의 이동은 화면 크기 대비 0.02% 미만이었다. 이 수치는 검사한 입력에서의 결과이며 모든 상호작용이 같은 픽셀로 유지됨을 보장하지 않는다. 비교 이미지를 열어 큰 흐름 유지와 경계 표현을 함께 확인했다.

### 성능

Windows의 Chrome headless, Radeon RX 9070 XT에서 native 4K App 루프를 비교했다. 원본은 511×287 격자, 구현본은 255×144 움직임 격자와 2048×1152 색상 격자를 사용한다.

- 최초 전체 루프 측정에서 CPU 프레임 처리 중앙값은 약 49.2ms → 11.4ms였다.
- 같은 255×144 격자에서 배열 교환 최적화만 비교하면 CPU step 중앙값은 약 11.8ms → 11.0ms였다.
- GPU 명령 제출 시간은 약 0.2ms였다. GPU 실행 완료 시간을 뜻하지 않는다.
- 주된 4K 개선은 움직임 격자 증가 제한에서 나온다. GPU 성능과 브라우저에 따라 실제 결과는 달라진다. 모바일 검사는 데스크톱 GPU에서의 에뮬레이션이며 실제 휴대전화 성능 측정은 아니다.

### 동작과 자원

- 실제 Next.js Vortex 페이지에서 데스크톱·모바일 입력, 메뉴, 슬라이더, 벡터, resize 및 홈 이동 뒤 렌더링 정지 검사를 통과했다.
- reset 시 색상 초기화, context loss 뒤 Canvas 전환을 확인했다. WebGL2를 사용할 수 없는 환경을 모의해 Canvas 출력에 유체 픽셀이 생성되는 것도 확인했다.
- 모바일 CDP 터치 이동에서 색상 픽셀 29,940개가 검출됐고 WebGL error는 0이었다.
- 네 번 resize 전후 자원은 texture 5, framebuffer 2, program 2로 일정했다. 홈 이동 뒤 추적 자원은 모두 0이었다.

## 다시 검사하기

```powershell
node scripts/verify-vortex-solver.mjs
node scripts/verify-vortex-4k.mjs <Playwright 설치 경로>/package.json
```

첫 스크립트는 백업 커밋 58afedf를 읽어 수치를 비교한다(Git 이력이 필요하다). 두 번째는 Chrome, TypeScript, 별도 Playwright 설치를 사용해 비교 이미지와 성능 JSON을 생성한다. 실제 페이지의 자원 검사는 로컬 프로덕션 서버를 켠 뒤 다음 명령으로 실행한다.

```powershell
node scripts/verify-gpu-resources.mjs <Playwright 설치 경로>/package.json Vortex
```

비교 이미지, 상세 결과 및 슬라이더로 비교할 수 있는 로컬 HTML은 `artifacts/vortex-4k/`에 보관한다. 생성된 자료는 Git에서 제외한다.
